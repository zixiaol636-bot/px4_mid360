# PX4 EKF2 indoor external-odometry parameter template
#
# Load path: QGroundControl -> Parameters -> Tools -> Load from file.
# PX4 parameter names change between firmware releases. Before loading this file,
# verify each parameter against the exact PX4 firmware running on your flight
# controller.
#
# This project sends FAST-LIO odometry through XRCE-DDS:
#   /fmu/in/vehicle_visual_odometry

# External vision / external odometry fusion.
EKF2_AID_MASK = 280          # Legacy PX4: fuse EV position, EV yaw, and EV velocity.
EKF2_HGT_MODE = 3            # Legacy PX4: use vision as height source.
EKF2_EV_DELAY = 0            # Tune for XRCE-DDS and onboard-computer timing.
EKF2_EV_POS_X = 0.10         # EV position observation noise X/Y/Z [m].
EKF2_EV_POS_Y = 0.10
EKF2_EV_POS_Z = 0.10
EKF2_EV_YAW = 0.10           # EV yaw observation noise [rad].
EKF2_EVP_NOISE = 0.20        # EV position noise [m].
EKF2_EVV_NOISE = 0.20        # EV velocity noise [m/s].
EKF2_EVP_GATE = 10.0         # EV position innovation gate.

# Indoor operation.
EKF2_MAG_TYPE = 5            # Disable magnetometer fusion in disturbed indoor areas.
EKF2_NOAID_NOISE = 0.5       # Position process noise without aiding [m].
EKF2_GBIAS_INIT = 0.0
EKF2_ABIAS_INIT = 0.0

# Offboard / commander.
COM_OBL_ACT = 2              # Offboard loss action: land.
COM_OF_LOSS_T = 0.5          # Offboard loss timeout [s].
COM_RCL_EXCEPT = 4           # Allow RC loss exception in Offboard if configured.
COM_ARM_WO_GPS = 1           # Allow arming without GPS.
NAV_RCL_ACT = 0              # RC loss action disabled for indoor test workflow.
NAV_DLL_ACT = 0              # Data-link loss action disabled for indoor test workflow.

# Safety switch. Only enable if your airframe and test procedure require it.
CBRK_IO_SAFETY = 22027
